#include<stdio.h>
int main()
{
    {
      printf("size of int %d\nsize of float %d\n size of char %d\nsize of double %d",sizeof(int),sizeof(float),sizeof(char),sizeof(double));
    }
    return(0);
}